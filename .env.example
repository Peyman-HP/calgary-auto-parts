PORT=4173

# Optional. If set, these override the SMTP values saved from the admin panel.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM=orders@calgaryroadready.ca

# Optional. If set, this overrides the owner email saved from the admin panel.
OWNER_EMAIL=owner@example.com

# Optional. If set, these override the Telegram values saved from the admin panel.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
